# Security Policy

## 报告安全问题

请不要在公开 Issue、Pull Request、讨论区或聊天截图中披露可利用的漏洞、API Key、微信凭据或用户数据。请通过仓库所有者提供的私密联系方式报告，并包含影响范围、复现步骤和建议修复方式。

## 凭据与私密数据

- DeepSeek Key 放在 `data/system/model.env`，不要写入 JSON 配置。
- 微信登录凭据保存在 `data/system/credentials.json`。
- `data/` 整体被 Git 忽略；不要使用强制添加将其提交。
- macOS/Linux 上模型密钥文件必须为 `0600`。
- 不要把密钥作为普通微信聊天内容发送，因为普通对话会进入租户历史。

如果凭据曾出现在终端历史、日志、提交、Issue 或任何共享位置，应立即在对应服务撤销或轮换。仅从当前工作区删除文本不能消除已经复制的凭据。

## 发布前检查

公开仓库前应：

1. 搜索真实 API Key、令牌、邮箱、用户名、租户编号、绝对路径和内部域名；
2. 确认 `data/`、`.venv/`、缓存、日志和测试产物未被跟踪；
3. 检查完整 Git 历史，而不只是当前文件；
4. 轮换任何曾进入历史的凭据；
5. 在无密钥、无 Ollama、无个人数据的干净环境运行测试。

## 支持范围

安全修复只保证应用于当前主分支。模型供应商、微信 iLink、浏览器、Codex 和操作系统的安全更新需要用户分别及时安装。
