[tool.ruff]
line-length = 120
target-version = "py310"
extend-exclude = ["data"]

[tool.ruff.lint]
select = ["E", "F", "W"]

[tool.mypy]
python_version = "3.10"
files = ["src"]
ignore_missing_imports = true
strict_optional = false
allow_untyped_globals = true
